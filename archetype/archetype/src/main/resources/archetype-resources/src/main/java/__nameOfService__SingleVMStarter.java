package ${package};

import ch.algotrader.starter.BaseStarter;

/**
 * This class starts the strategy in Live Trading Mode in a single VM
 *
 */
public class ${artifactId.substring(0,1).toUpperCase()}${artifactId.substring(1)}SingleVMStarter extends BaseStarter  {

}
