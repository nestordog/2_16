@Name('GET_LAST_TICK')
@Priority(4)
select
    tick.security.id as securityId,
    tick.* as tick
from
    Tick.std:groupwin(security.id).win:length(1) as tick
where
    tick.security is not null
and
    tick.dateTime.time > (current_timestamp - DateUtil.getDuration('ONE_WEEK'))
order by
    tick.security.id;
