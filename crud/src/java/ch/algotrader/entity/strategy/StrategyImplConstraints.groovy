constraints = {
    name(blank: false)
    autoActivate()
    allocation(blank: false)
    initModules(widget: 'textarea')
    runModules(widget: 'textarea')
    props()
    portfolioValues(display: false)
    cashBalances(display: false)
    measurements(display: false)
}
